using Avalonia.Controls;

namespace MetalLink.Desktop.Views;

/// <summary>
/// A DataGrid subclass used as a marker for applying animations via XAML styles.
/// Inherits all DataGrid functionality without adding animation logic in code-behind.
/// Animations are applied via Transitions in XAML styles.
/// </summary>
public class AnimatedDataGrid : DataGrid
{
    // Placeholder - animations are handled via XAML Transitions
}
